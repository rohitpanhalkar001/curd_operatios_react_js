import { combineReducers } from "redux";

import AllUserScreenReducers from './AllUserScreenReducers'

const RootReducer = combineReducers({
    AllUserScreenReducers,
});

export default RootReducer;
